---
- hosts: all
  become: yes
  vars:
    username: test1

  tasks:
  - name: Deleting the user {{ username }}
    user:
      name: "{{ username }}"
      state: absent
      remove: yes
      force: yes
